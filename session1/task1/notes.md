The error message when running the C code is a lot less clearer, as it can implicate a number of things. In comparison, the python error message states the exact issue occuring, which is that the index is out of the range of the array x.

Therefore, Python would be the easier language to debug this code in.